package org.agilewiki.jactor2.core.blades.transmutable.tssmTransactions;

import org.agilewiki.jactor2.core.blades.transmutable.transactions.TransmutableSource;

import java.util.SortedMap;

/**
 * A TSSMap source.
 */
abstract public class TSSMSource<VALUE> extends TransmutableSource<SortedMap<String, VALUE>, TSSMap<VALUE>> {
}
